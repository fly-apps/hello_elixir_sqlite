#!/bin/sh
# We dont need to run migration on SQLite upon deploy we run when the server starts up
# Look into entrypoint.sh

# cd -P -- "$(dirname -- "$0")"
# exec ./hello_elixir eval HelloElixir.Release.migrate
